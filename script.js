
/*console.log('hello!')

add(
  if(
    empty(string(items('Apply_to_each_Program_Report')?['flowCount'])),
    int(1),
    int(items('Apply_to_each_Program_Report')?['flowCount']))
  ,1)

  add(if(empty(string(items('Apply_to_each_Program_Report')?['flowCount'])),int(1),int(items('Apply_to_each_Program_Report')?['flowCount'])),1)
  